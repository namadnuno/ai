---
name: rule-name
description: One-sentence summary. Used by list_rules — must be self-explanatory so Claude can decide whether to pull body.
globs: ["src/**/*.ts"]
---

# Rule body

Explain the rule. Why it exists. Examples of correct usage. Anti-patterns.

Keep concrete. Generic statements ("write clean code") get ignored.
